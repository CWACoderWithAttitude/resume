services:
  jekyll:
    container_name: online-cv
    image: jekyll/builder:latest #4.0
    command: jekyll serve --watch --force_polling --verbose --livereload --host 0.0.0.0
    volumes:
      - .:/srv/jekyll:Z
    environment:
      JEKYLL_VERSION: 4.3
    ports:
      - 4000:4000
